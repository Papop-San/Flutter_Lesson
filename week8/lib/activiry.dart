import 'package:flutter/material.dart';

class ActivatyPage extends StatelessWidget {
  const ActivatyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text("ActivityPage"),
      ),
    );
  }
}
