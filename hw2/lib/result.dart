import 'package:flutter/material.dart';

class fromResult extends StatelessWidget {
  const fromResult({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
